apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "plex-linker.fullname" . }}-test-connection"
  labels:
    {{- include "plex-linker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "plex-linker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
