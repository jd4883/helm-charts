apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.configMap.name }}
  namespace: {{ .Release.Namespace }}
data:
  {{- range $key, $val := .Values.env.normal }}
  {{ $key }}: {{ $val }}
  {{- end}}
