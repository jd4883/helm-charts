apiVersion: v1
kind: Service
metadata:
  name: {{ include "plex-linker.fullname" . }}
  labels:
    {{- include "plex-linker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "plex-linker.selectorLabels" . | nindent 4 }}
